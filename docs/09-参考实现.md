# 09 参考实现（可直接照抄的草稿）

> 本文档把规范落成**可直接使用的代码草稿**：P0/P1 阶段照抄即可，允许按实际情况微调。
> 冲突优先级：`03-设计系统` 的令牌值 > 本文档草稿 > 其他描述性规范。

## 1. tokens.css（完整草稿）

```css
/* tokens.css —— 与 03-设计系统.md 保持一致 */
:root {
  /* 颜色：中性 */
  --color-bg: #FAFAF8;
  --color-surface: #FFFFFF;
  --color-surface-muted: #F3F2EC;
  --color-border: #E8E8E4;
  --color-border-strong: #D6D5CE;
  --color-text: #1F1F1D;
  --color-text-secondary: #6E6E68;
  --color-text-tertiary: #A8A69E; /* 仅装饰性文字 */

  /* 颜色：强调（陶土橙） */
  --color-accent: #B05C3A;
  --color-accent-hover: #97492C;
  --color-accent-soft: #F6E9E1;

  /* 颜色：状态徽章 */
  --color-dev-text: #A74A1F; --color-dev-bg: #FBEBDD;
  --color-done-text: #336B43; --color-done-bg: #E6F0E8;
  --color-arch-text: #6F6F6A; --color-arch-bg: #EFEFEB;
  --color-exp-text: #5A4B8A;  --color-exp-bg: #ECE9F4;

  /* 字体 */
  --font-sans: -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC",
    "Hiragino Sans GB", "Microsoft YaHei", "Noto Sans SC", sans-serif;
  --font-display: Georgia, "Songti SC", "Noto Serif SC", serif;
  --font-mono: ui-monospace, "SF Mono", "Cascadia Code", Consolas, monospace;

  /* 字号（含行高/字重） */
  --text-display: 40px; --lh-display: 1.2;
  --text-h1: 32px; --lh-h1: 1.25;
  --text-h2: 24px; --lh-h2: 1.35;
  --text-h3: 20px; --lh-h3: 1.4;
  --text-body: 16px; --lh-body: 1.7;
  --text-small: 14px; --lh-small: 1.6;
  --text-caption: 13px; --lh-caption: 1.5;

  /* 间距 */
  --space-1: 4px; --space-2: 8px; --space-3: 12px; --space-4: 16px;
  --space-6: 24px; --space-8: 32px; --space-12: 48px; --space-16: 64px;

  /* 圆角 / 阴影 / 边框 */
  --radius-sm: 4px; --radius-md: 8px; --radius-lg: 12px;
  --shadow-card: 0 1px 2px rgba(0, 0, 0, 0.04);
  --shadow-card-hover: 0 2px 8px rgba(0, 0, 0, 0.06);
  --border-default: 1px solid var(--color-border);

  /* 布局 */
  --container: 960px;
  --container-narrow: 720px;
  --bp-sm: 640px;
  --bp-md: 1024px;
  --grid-gap: 24px;

  /* 动效 */
  --dur-fast: 150ms;
  --dur-base: 250ms;
  --dur-slow: 400ms;
  --ease-standard: cubic-bezier(0.2, 0, 0, 1);
  --ease-out: cubic-bezier(0, 0, 0.2, 1);
}
```

## 2. base.css（关键段）

```css
/* reset 最小集 */
*, *::before, *::after { box-sizing: border-box; }
html { -webkit-text-size-adjust: 100%; }
body {
  margin: 0;
  background: var(--color-bg);
  color: var(--color-text);
  font-family: var(--font-sans);
  font-size: var(--text-body);
  line-height: var(--lh-body);
  text-rendering: optimizeLegibility;
}
h1, h2, h3, h4, p, ul, ol { margin: 0; }
a { color: var(--color-accent); text-decoration: none; }
a:hover { color: var(--color-accent-hover); text-decoration: underline; }

/* 焦点 */
:focus-visible { outline: 2px solid var(--color-accent); outline-offset: 2px; }

/* 选中色 */
::selection { background: var(--color-accent-soft); }

/* 布局容器 */
.container { max-width: var(--container); margin-inline: auto; padding-inline: clamp(16px, 4vw, 32px); }
.container--narrow { max-width: var(--container-narrow); }

/* 页面过渡（PageTransition 配套） */
.page-enter-active, .page-leave-active { transition: opacity var(--dur-fast) var(--ease-standard), transform var(--dur-fast) var(--ease-standard); }
.page-enter-from { opacity: 0; transform: translateY(8px); }
.page-leave-to { opacity: 0; }

/* 减动效 */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    transition-duration: 0.01ms !important;
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
  }
}

/* 打印样式：名片式输出（About/项目页可直接打印为简历） */
@media print {
  header, footer, nav { display: none !important; }
  body { background: #fff; font-size: 12px; }
  .container { max-width: none; padding: 0; }
  a { color: inherit; text-decoration: none; }
  .no-print { display: none !important; }
}
```

## 3. markdown.css（.article 作用域）

```css
.article { font-size: var(--text-body); line-height: var(--lh-body); }
.article > * + * { margin-top: 1.2em; }
.article h2 { font-size: var(--text-h2); line-height: var(--lh-h2); font-weight: 600; margin-top: 2.2em; }
.article h2:first-child { margin-top: 0; }
.article h3 { font-size: var(--text-h3); font-weight: 600; margin-top: 1.6em; }
.article ul, .article ol { padding-left: 1.4em; }
.article li + li { margin-top: 6px; }
.article strong { font-weight: 600; }

/* 行内代码：纸张感（非暗色终端） */
.article code {
  font-family: var(--font-mono);
  font-size: 0.88em;
  background: var(--color-surface-muted);
  border: 1px solid var(--color-border);
  border-radius: 4px;
  padding: 0.1em 0.35em;
}
/* 代码块 */
.article pre {
  background: var(--color-surface-muted);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: 16px;
  overflow-x: auto;
  font-size: 13.5px;
  line-height: 1.6;
}
.article pre code { background: none; border: none; padding: 0; font-size: inherit; }
/* 代码块语言角标（可选用 pre 上方 ::before 输出语言名，v1 简化为不做） */

/* hljs 亮色主题：覆盖默认暗色（色板 03 §2.4；未标注语言时无 .hljs 配色） */
.article .hljs-keyword, .article .hljs-selector-tag { color: #A74A1F; }
.article .hljs-string, .article .hljs-attr, .article .hljs-attribute { color: #336B43; }
.article .hljs-title, .article .hljs-title.function_ { color: #5A4B8A; }
.article .hljs-comment, .article .hljs-quote { color: #6F6F6A; }
.article .hljs-number, .article .hljs-literal, .article .hljs-built_in { color: #1F1F1D; }
.article .hljs-variable, .article .hljs-params, .article .hljs-name { font-style: italic; }

/* 引用 */
.article blockquote {
  margin: 0; padding-left: 16px;
  border-left: 2px solid var(--color-border-strong);
  color: var(--color-text-secondary);
}
/* 表格 */
.article table { width: 100%; border-collapse: collapse; font-size: var(--text-small); }
.article th, .article td { border: 1px solid var(--color-border); padding: 8px 12px; text-align: left; }
.article th { background: var(--color-surface-muted); font-weight: 600; }
/* 图片 */
.article img { max-width: 100%; border-radius: var(--radius-md); height: auto; }
/* 分隔线 */
.article hr { border: none; border-top: 1px solid var(--color-border); }
```

## 4. 数据层实现草稿（meta-only 设计，详见 `14-技术架构设计` §5）

### 4.1 utils/parse-frontmatter.js（主 bundle 内，轻量解析器）

```js
// 受控子集解析：单行 key: value；字符串/布尔/整数/简单字符串数组/空值。
// 越界内容由 scripts/validate-content.mjs（规则 14）拦截，构建期报错。
export function parseFrontmatter(raw) {
  const m = /^---\r?\n([\s\S]*?)\r?\n---\r?\n?/.exec(raw)
  if (!m) throw new Error('frontmatter missing')
  const meta = {}
  for (const line of m[1].split(/\r?\n/)) {
    const t = line.trim()
    if (!t || t.startsWith('#')) continue
    const i = t.indexOf(':')
    const key = t.slice(0, i).trim()
    let v = t.slice(i + 1).trim()
    if (v === '') { meta[key] = null; continue }
    if (v.startsWith('[') && v.endsWith(']')) {
      meta[key] = v.slice(1, -1).split(',').map(s => s.trim().replace(/^["']|["']$/g, '')).filter(Boolean)
    } else if (v === 'true') meta[key] = true
    else if (v === 'false') meta[key] = false
    else if (/^-?\d+$/.test(v)) meta[key] = Number(v)
    else meta[key] = v.replace(/^["']|["']$/g, '')
  }
  return { meta, raw: raw.slice(m[0].length).trimStart() }
}
```

### 4.2 utils/content.js（meta-only，不 import markdown-it）

```js
import { parseFrontmatter } from './parse-frontmatter.js'

const globProjects = import.meta.glob('../content/projects/*.md', { query: '?raw', import: 'default', eager: true })
const globNotes = import.meta.glob('../content/notes/*.md', { query: '?raw', import: 'default', eager: true })

function normalizeMeta(meta) {
  return {
    ...meta,
    featured: !!meta.featured,                 // 布尔归一（缺省 false）
    draft: !!meta.draft,
    tech: Array.isArray(meta.tech) ? meta.tech : [],   // 数组归一（缺省/空 → []）
    tags: Array.isArray(meta.tags) ? meta.tags : [],
    demo: meta.demo || undefined,              // 空值统一为 undefined
    github: meta.github || undefined,
    order: meta.order || undefined,
  }
}

function buildMeta(entries) {
  return entries
    .filter(([path]) => !path.includes('_'))
    .map(([path, raw]) => {
      const { meta } = parseFrontmatter(raw)
      return { slug: path.split('/').pop().replace(/\.md$/, ''), meta: normalizeMeta(meta) }
    })
    .filter((item) => !item.meta.draft)
}

export const projects = buildMeta(Object.entries(globProjects))
  .sort((a, b) => (a.meta.order ?? 999) - (b.meta.order ?? 999) || b.meta.date.localeCompare(a.meta.date))
export const notes = buildMeta(Object.entries(globNotes))
  .sort((a, b) => b.meta.date.localeCompare(a.meta.date))

export const getProject = (slug) => projects.find((p) => p.slug === slug)
export const getNote = (slug) => notes.find((n) => n.slug === slug)

export const featuredProjects = projects.filter((p) => p.meta.featured).slice(0, 3)
export const recentNotes = notes.slice(0, 3)

export const techList = [...new Set(projects.flatMap((p) => p.meta.tech))].sort((a, b) => a.localeCompare(b, 'zh'))
export const noteTags = [...new Set(notes.flatMap((n) => n.meta.tags))].sort((a, b) => a.localeCompare(b, 'zh'))

// site.json / lab.json / timeline.json 同样用 glob（?raw）导入后 JSON.parse
// export const site = JSON.parse(rawSite)

// 原文懒加载（详情页用；非 eager → 进详情共享 chunk）
export const projectRawFiles = Object.fromEntries(
  Object.entries(import.meta.glob('../content/projects/*.md', { query: '?raw', import: 'default' }))
    .filter(([path]) => !path.includes('_'))
    .map(([path, loader]) => [path.split('/').pop().replace(/\.md$/, ''), loader])
)
export const noteRawFiles = /* 同结构 */
```

### 4.3 utils/markdown.js（详情 chunk，动态 import）

```js
import MarkdownIt from 'markdown-it'
import hljs from 'highlight.js/lib/common'

const md = new MarkdownIt({
  html: false,
  linkify: true,
  highlight(str, lang) {
    if (lang && hljs.getLanguage(lang)) {
      return `<pre><code class="hljs language-${lang}">${hljs.highlight(str, { language: lang }).value}</code></pre>`
    }
    return `<pre><code>${md.utils.escapeHtml(str)}</code></pre>`
  },
})

// 外链（站外 http(s)）一律新标签打开；站内绝对路径（/projects、/notes 开头）走 SPA 导航
const defaultLinkOpen = md.renderer.rules.link_open || ((tokens, idx, options, env, self) => self.renderToken(tokens, idx, options))
md.renderer.rules.link_open = (tokens, idx, options, env, self) => {
  const href = tokens[idx].attrGet('href') ?? ''
  if (/^https?:\/\//.test(href)) {
    tokens[idx].attrSet('target', '_blank')
    tokens[idx].attrSet('rel', 'noopener noreferrer')
  }
  return defaultLinkOpen(tokens, idx, options, env, self)
}

// h2 锚点：为 ## 标题生成稳定 id（格式 <prefix>-<n>，prefix 建议传 slug）
// 计数器每次渲染重置 → id 与渲染历史无关，同文恒等（跨文无累积）
let idPrefix = 's'
let idCounter = 0
md.core.ruler.push('h2_anchor', (state) => {
  for (const token of state.tokens) {
    if (token.type === 'heading_open' && token.tag === 'h2') {
      token.attrSet('id', `${idPrefix}-${++idCounter}`)
    }
  }
})

export function renderMarkdown(raw, prefix = 's') {
  idPrefix = prefix
  idCounter = 0
  return md.render(raw)
}
```

### 4.4 详情页加载时序（样板）

```js
// pages/ProjectDetail.vue（逻辑骨架）
const project = getProject(route.params.slug)          // 同步 meta，可先渲染页头
const html = ref('')
if (project) {
  const loader = projectRawFiles[project.slug]
  if (loader) {
    const [raw, mod] = await Promise.all([loader(), import('@/utils/markdown.js')])
    html.value = mod.renderMarkdown(raw, project.slug)
  }
}
```

> 注：`import('@/utils/markdown.js')` 与原文 loader 同属一个懒加载链，Vite 自动共享 chunk。

### 4.5 utils/query.js（URL 工具，零依赖）

```js
// vue-router 查询值形态：单值 string | 重复参数 string[] | 缺失 undefined | 空串 ''
// toQueryArray 统一归一为 string[]（§5.4 契约）
export function toQueryArray(v) {
  if (v == null) return []
  if (Array.isArray(v)) return v.filter(Boolean)
  return v === '' ? [] : [v]
}
```

### 4.6 utils/seo.js（document 元信息封装，§5.7）

```js
import { site } from './content.js'

export function setTitle(t) {
  if (!t) return
  document.title = t.length > 60 ? t.slice(0, 60) : t        // 02 §8：≤ 60
}
export function setDescription(d) {
  const text = (d || site.bio).slice(0, 120)                 // 02 §8：≤ 120
  let el = document.querySelector('meta[name="description"]')
  if (!el) {
    el = document.createElement('meta')
    el.name = 'description'
    document.head.appendChild(el)
  }
  el.content = text
}
export function setCanonical(url) { /* v1.1 启用，签名预留（14 §9 v1.1） */ }

// afterEach 统一入口：静态路由用 meta.title/description（函数求值）；详情路由无 meta 定义 → 跳过，组件内 watchEffect 覆盖（14 §5.3/§5.7）
export function applyMeta(route) {
  const meta = route.meta || {}
  if (meta.title) setTitle(typeof meta.title === 'function' ? meta.title(site) : meta.title)
  if (meta.description) setDescription(typeof meta.description === 'function' ? meta.description(site) : meta.description)
}
```

### 4.7 scripts/validate-content.mjs（L1 校验骨架，§6.4）

```js
// Node ≥ 18；退出码 0 通过 / 1 失败。规则清单 04 §9；检查方法 14 §6.4 表。
// frontmatter 解析复用浏览器同款实现，杜绝双实现漂移（依赖 package.json "type": "module"）。
import { readdirSync } from 'node:fs'
import { join } from 'node:path'
import { parseFrontmatter } from '../src/utils/parse-frontmatter.js'

const ROOT = 'src/content'
const errors = []
const report = (rule, file, msg) => errors.push(`ERROR [规则${rule}] ${file}: ${msg}`)

function walk(dir) {           // _ 开头文件/目录一律跳过（模板不入管线）
  return readdirSync(dir, { withFileTypes: true }).flatMap((e) =>
    e.name.startsWith('_') ? [] : (e.isDirectory() ? walk(join(dir, e.name)) : [join(dir, e.name)]))
}

// 围栏切分：正文 → [非代码, 代码, 非代码, …]；仅非代码段参与规则 15（代码里含 < 合法）
function nonCodeParts(body) {
  const parts = []
  let inCode = false, cur = ''
  for (const line of body.split('\n')) {
    if (/^```/.test(line.trim())) { inCode = !inCode; if (!inCode && cur) { parts.push(cur); cur = '' } ; continue }
    if (!inCode) cur += line + '\n'
  }
  if (cur) parts.push(cur)
  return parts
}

// 规则 1–14 按 14 §6.4 表逐条实现（枚举、slug 冲突 Set、段落清单、URL、JSON schema 断言…）
// 规则 15 示例：
function checkRule15(file, body) {
  for (const part of nonCodeParts(body)) {
    const m = /<[a-zA-Z!/]/.exec(part)
    if (m) report(15, file, `非代码内容含 HTML 标签"${m[0]}"；如需展示请包进 \`\`\` 围栏`)
  }
}

// 收尾：全部检查完一次性打印（不 fail-fast），末尾汇总
// if (errors.length) { console.error(errors.join('\n')); console.error(`共 ${errors.length} 个错误`); process.exit(1) }
// console.log('validate: OK')
```

### 4.8 index.html 骨架（含 noscript 静态降级，§7.5）

```html
<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="theme-color" content="#FAFAF8" />
  <title>Yuer Studio</title>                    <!-- 运行时由 seo.js 覆盖 -->
  <meta name="description" content="做软件的 Yuer。关注 AI 应用与工程化。" />
  <link rel="icon" type="image/svg+xml" href="favicon.svg" />   <!-- 相对路径：随 base 生效 -->
  <meta property="og:title" content="Yuer Studio" />
  <meta property="og:description" content="做软件的 Yuer。关注 AI 应用与工程化。" />
  <meta property="og:type" content="website" />
  <meta property="og:image" content="og-image.png" />  <!-- 相对路径随页面解析；自定义域名后同值可用 -->
  <script type="application/ld+json">
  { "@context": "https://schema.org", "@type": "Person",
    "name": "Yuer", "url": "https://<user>.github.io/<repo>/",
    "sameAs": ["https://github.com/anyuer678"], "jobTitle": "Software Developer" }
  </script>
</head>
<body>
  <div id="app"></div>
  <noscript>
    <p>本站需要 JavaScript 才能完整渲染。</p>
    <ul>
      <li><a href="./">首页</a></li>
      <li><a href="./projects">项目</a></li>
      <li><a href="./notes">笔记</a></li>
      <li><a href="./about">关于</a></li>
    </ul>
  </noscript>
  <script type="module" src="/src/main.js"></script>
</body>
</html>
```

> 路径注意：部署在 `<repo>/` 子路径时，静态资源用**相对路径**（`favicon.svg` / `og-image.png` / `./projects`），绝对路径 `/xxx` 会指到站点根导致 404；自定义域名（base=/）后两者等价。

## 5. 关键组件结构草稿

### TagRotator.vue（标签轮换）

```vue
<script setup>
import { ref, computed, onMounted, onBeforeUnmount } from 'vue'

const props = defineProps({
  tags: { type: Array, default: () => [] },
  interval: { type: Number, default: 500 },
})

const index = ref(0)
const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches
let timer

onMounted(() => {
  if (props.tags.length > 1 && !reduced) {
    timer = setInterval(() => { index.value = (index.value + 1) % props.tags.length }, props.interval)
  }
})
onBeforeUnmount(() => clearInterval(timer))

const current = computed(() => props.tags[index.value] ?? '')
</script>

<template>
  <span class="tag-rotator" aria-live="off">
    <Transition name="fade" mode="out-in">
      <span :key="current" class="tag-rotator__word">{{ current }}</span>
    </Transition>
  </span>
</template>

<style scoped>
.tag-rotator__word { font-family: var(--font-mono); font-size: var(--text-small); color: var(--color-text-secondary); }
.fade-enter-active, .fade-leave-active { transition: opacity 200ms var(--ease-standard); }
.fade-enter-from, .fade-leave-to { opacity: 0; }
</style>
```

### ProjectCard.vue 结构（骨架，样式照 06 布局 + 03 令牌）

```vue
<script setup>
defineProps({
  project: { type: Object, required: true },
  featured: { type: Boolean, default: false },
})
</script>

<template>
  <article class="project-card" :class="{ 'project-card--featured': featured }">
    <RouterLink :to="`/projects/${project.slug}`" class="project-card__link">
      <header class="project-card__head">
        <h3 class="project-card__title">{{ project.title }}</h3>
        <StatusBadge :status="project.status" />
      </header>
      <p v-if="project.subtitle" class="project-card__subtitle">{{ project.subtitle }}</p>
      <p class="project-card__summary">{{ project.summary }}</p>
      <div class="project-card__tech">
        <Tag v-for="t in project.tech" :key="t" :label="t" />
      </div>
      <footer class="project-card__foot">
        <ExternalLink v-if="project.github" :href="project.github">GitHub</ExternalLink>
        <ExternalLink v-if="project.demo" :href="project.demo">Demo</ExternalLink>
      </footer>
    </RouterLink>
  </article>
</template>
```

## 6. 与纲要的关系说明

本文档是"实施草稿"而非"新规范"：tokens 值以 `03-设计系统` 为准；组件结构允许在实现中调整（props/events 变更需回写 `05-组件规范`）；管线逻辑以本文件为起点，如遇性能或可维护性问题可改实现不改数据契约。

各节对应：§1–§3 样式三件套（对接 `14` §5.5）；§4 数据层与工程件（对接 `14` §5.1–§5.7/§6.4/§7.1/§7.5）；§5 组件草稿（对接 `05-组件规范`）。
